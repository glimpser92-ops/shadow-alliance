services:
  - type: web
    name: shadow-alliance-classroom
    env: node
    buildCommand: npm ci
    startCommand: npm start
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
